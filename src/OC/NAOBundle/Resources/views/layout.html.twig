<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  {% block title %}{% endblock %}
  {% block stylesheets %}
  <!-- jQuery -->
  <script src="{{asset("assets/jquery/js/jquery-3.2.1.min.js")}}"></script>
	<script src="{{asset("assets/jquery/js/jquery-ui.min.js")}}"></script>
	<!-- Bootstrap -->
	<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <!-- CSS -->
  <link rel="stylesheet" href="{{asset("assets/jquery/css/jquery-ui.css")}}">
  <link rel="stylesheet" href="{{asset("assets/css/map.css")}}">
  <link rel="stylesheet" href="{{asset("assets/css/main.css")}}">
  <link rel="stylesheet" href="{{asset("assets/css/profil.css")}}">
  <link rel="stylesheet" href="{{asset("assets/jquery/css/jquery-ui.structure.css")}}">
  <link rel="stylesheet" href="{{asset("assets/jquery/css/jquery-ui.theme.css")}}">
  <!-- Font -->
  <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
  <script defer src="https://use.fontawesome.com/releases/v5.0.4/js/all.js"></script>
  <!-- DatePicker -->
  <link rel="stylesheet" href="{{asset("assets/datepicker/css/bootstrap-datepicker3.min.css")}}">
  <script src="{{asset("assets/datepicker/js/bootstrap-datepicker.js")}}"></script>
  <script src="{{asset("assets/datepicker/locales/bootstrap-datepicker.fr.min.js")}}"></script>

  {% endblock %}

</head>
  <body>
    <nav class="navbar navbar-default hidden-xs navbar-fixed-top"> <!-- Navbar sur ordinateur -->
      <div class="navigation">
        <div class="col-md-9">
          <div class="container-fluid">
            <div class="navbar-header">
              <a href="{{ path('ocnao_homepage') }}"><img class="logomenu" src="{{ asset("assets/images/Logomenu2.png")}}"/></a>
            </div>
            <div class="collapse navbar-collapse">
               <ul class="nav navbar-nav">
                  <li role="presentation"><a href="{{ path('ocnao_recherche') }}">RECHERCHER UNE OBSERVATION</a></li>
                  <li role="presentation"><a href="{{ path('ocnao_addObservation') }}">AJOUTER UNE OBSERVATION</a></li>
                  {% if is_granted("ROLE_ADMIN") or is_granted("ROLE_NATURALIST") %}
                    <li role="presentation"><a href="{{ path('ocnao_profil_observationatvalidate') }}">OBSERVATIONS À VALIDER</a></li>
                  {% endif %}
                  <li role="presentation"><a href="#">COMMENT CA MARCHE ?</a></li>
                  <li role="presentation"><a href="{{ path('ocnao_contact') }}">NOUS CONTACTER</a></li>
               </ul>
            </div>
          </div>
        </div>
        <div class="col-md-3">
          {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
          <ul class="nav navbar-nav navbar-right">
            <li role="presentation"><a href="#">Faire un don</a></li>
          </ul>
          <div class="dropdown nav navbar-nav navbar-right">
            <button class="btn navbar-btn dropdown-toggle" type="button" id="dropdownMenu2" data-toggle="dropdown" aria-expanded="true">
              <img class="userIcone" src="{{ asset("assets/images/userIcone.png")}}" /> {{ app.user.username }}
            </button>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenu2">
              {% if is_granted("ROLE_ADMIN") %}
                <li role="presentation"><a role="menuitem" tabindex="-1" href="{{ path('ocnao_profil_users') }}">LISTE DES UTILISATEURS</a></li>
              {% endif %}
              <li role="presentation"><a role="menuitem" tabindex="-1" href="{{ path('ocnao_profil_userobservation') }}">MES OBSERVATIONS</a></li>
              <li role="presentation"><a role="menuitem" tabindex="-1" href="{{ path('ocnao_profil_parameter') }}">MES PARAMÈTRES</a></li>
              <li role="presentation"><a role="menuitem" tabindex="-1" href="{{ path('fos_user_security_logout') }}">DÉCONNEXION</a></li>
            </ul>
          </div>
          {% else %}
          <ul class="nav navbar-nav navbar-right">
            <li role="presentation"><a href="{{ path('fos_user_registration_register') }}">S'inscrire</a></li>
            <li role="presentation"><a href="{{ path('fos_user_security_login') }}" >Se connecter</a></li>
            <li role="presentation"><a href="#">Faire un don</a></li>
          </ul>
          {% endif %}
        </div>
      </div>
    </nav> <!-- Fin navbar sur ordinateur -->

    <nav class="nav navbar navbar-default visible-xs navbar-fixed-top"> <!-- Navbar sur Smartphone -->
      <div class="">
        <div class="col-xs-12">
          <div class="container-fluid">
            <div class="navbar-header">
              <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".menu" aria-expanded="false" data-placement="bottom" >
                   <span class="icon-bar"></span>
                   <span class="icon-bar"></span>
                   <span class="icon-bar"></span>
              </button>
              {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <a class="btn btn-default navbar-toggle" href="#">Faire un don</a>
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".user" aria-expanded="false" data-placement="bottom" >
                  <img class="userIcone" src="{{ asset("assets/images/userIcone.png")}}" />
                </button>
              {% else %}
                  <a class="btn btn-default navbar-toggle" href="#">Faire un don</a>
                  <a class="btn btn-default navbar-toggle" href="{{ path('fos_user_registration_register') }}">S'inscrire</a>
                  <a class="btn btn-default navbar-toggle" href="{{ path('fos_user_security_login') }}" >Se connecter</a>
              {% endif %}
              <a href="{{ path('ocnao_homepage') }}"><img class="logomenu" src="{{ asset("assets/images/Logomenu2.png")}}"/></a>
            </div>
            <div class="collapse navbar-collapse menu center">
               <ul class="nav navbar-nav">
                 <h3 class="collapse-header">MENU</h3>
                 <li role="presentation"><a href="{{ path('ocnao_recherche') }}">RECHERCHER UNE OBSERVATION</a></li>
                 <li role="presentation"><a href="{{ path('ocnao_addObservation') }}">AJOUTER UNE OBSERVATION</a></li>
                 {% if is_granted("ROLE_ADMIN") %}
                  <li role="presentation"><a href="#">OBSERVATIONS À VALIDER</a></li>
                 {% endif %}
                 <li role="presentation"><a href="#">COMMENT CA MARCHE ?</a></li>
                 <li role="presentation"><a href="{{ path('ocnao_contact') }}">NOUS CONTACTER</a></li>
               </ul>
            </div>
            <div class="collapse navbar-collapse user center">
              <ul class="nav navbar-nav">
              <h3 class="collapse-header">PANEL UTILISATEUR</h3>
              {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                {% if is_granted("ROLE_ADMIN") %}
                  <li role="presentation"><a role="menuitem" tabindex="-1" href="{{ path('ocnao_profil_users') }}">LISTE DES UTILISATEURS</a></li>
                {% endif %}
                <li role="presentation"><a role="menuitem" tabindex="-1" href="{{ path('ocnao_profil_userobservation') }}">MES OBSERVATIONS</a></li>
                <li role="presentation"><a role="menuitem" tabindex="-1" href="{{ path('ocnao_profil_parameter') }}">MES PARAMÈTRES</a></li>
                <li role="presentation"><a href="{{ path('fos_user_security_logout') }}">DÉCONNEXION</a></li>
              {% else %}
                <li role="presentation"><a href="{{ path('fos_user_registration_register') }}">S'inscrire</a></li>
                <li role="presentation"><a href="{{ path('fos_user_security_login') }}" >Se connecter</a></li>
              {% endif %}
              </ul>
            </div>
          </div>
        </div>
      </div>
    </nav> <!-- Fin navbar sur Smartphone -->

<div class="container" style="margin-top:30px"> <!-- Messages d'informations -->
  {% for label, messages in app.flashes(['info', 'success', 'danger']) %}
    {% for message in messages %}
      <div class="alert alert-{{ label }}">
        {{ message }}
      </div>
    {% endfor %}
  {% endfor %}
</div>

{% block body %}
  {% if block("user") is defined %}
  <div class="container">
    <div >
      {% block user %}{% endblock %}
    </div>
  </div>
  {% endif %}
{% endblock %}
<script src="{{asset("assets/js/adaptation.js")}}"></script>

</body>
<footer>


  <div class="footer container"> <!-- Footer du site -->
    <div class="col-md-offset-4 col-md-4">
      <div class="center"> <!-- reseaux sociaux -->
        <div class="row">
          <a href="https://www.facebook.com" target="_blank"><img src="{{ asset("assets/images/fb_logo.png")}}"></img></a>
          <a href="https://www.twitter.com" target="_blank"><img src="{{ asset("assets/images/twitter_logo.png")}}"></img></a>
        </div>
        <div class="row">
          <p>ABONNEZ-VOUS À NOTRE NEWSLETTER</p>
          <input type="email" name="email" value="" placeholder="Entrez votre adresse mail"></input>
        </div>
        <div class="row col-md-12 col-xs-12">
          <div class="col-xs-4">
            <a href="{{ path('ocnao_addObservation') }}">COMMENT CA MARCHE ?</a>
          </div>
          <div class="col-xs-4 border_side">
            <a href="{{ path('ocnao_contact') }}">NOUS CONTACTER</a>
          </div>
          <div class="col-xs-4">
            <a href="#">MENTION LÉGALES</a>
          </div>
        </div>
      </div>
    </div>
  </div> <!-- Fin footer du site -->
</footer>

</html>
